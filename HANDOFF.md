# Claude Code 인계 메모

업데이트: 2026-09-20 23:55 KST

## 시작할 때

1. `AGENTS.md`와 `SPEC.md`를 먼저 읽는다.
2. `codex/ux-fed-rate` 브랜치를 체크아웃한다.
3. 이 브랜치는 아직 원격 push·Worker 배포·main 병합을 하지 않았다.

현재 main 기준점은 `e674711`이고, 이 브랜치에는 다음 두 커밋이 있다.

- `a450a2c fix: separate Fed target range from EFFR`
- `a865046 feat: refine portfolio and investment opinion UX`

## 완료된 변경

- 미국 정책금리는 FRED `DFEDTARL`·`DFEDTARU` 목표범위로 표시하고, EFFR(`FEDFUNDS`)은 거시 상세의 별도 지표로 분리했다. 새 마이그레이션은 `0011_fed_target_range.sql`이다.
- 짧은 밸류에이션 이력 기간 표시와 혼합 시장 시세 시각 범위를 보완했다.
- 포트폴리오 위험 영역에서 비중 상한의 불필요한 비차단 문구를 제거했다. 기대수익률 편집은 기본 접힘, 긴 계산 설명은 `?` 팝오버다.
- 보유 수량은 저장값을 건드리지 않고 정수에 매우 가까울 때 화면에서 정수로 표시한다.
- 종목 탭은 `보유`·`관심`을 상단에서 즉시 전환한다. 검색 결과의 긴 종목명은 줄바꿈해 전부 보인다.
- 탭 이름을 `투자의견`으로 바꾸고 첫 기록은 `커버리지 개시`, 이후 새 기록은 `투자의견 업데이트`로 구분했다. 종목 상세에는 등급·목표가 변천 타임라인이 있다.
- 전역 `+`의 `판정`을 `기술적 분석`으로 바꾸고 설명은 `기술적 지표로 매매 적합도 확인`으로 바꿨다.
- 네이티브 `<select>`를 공통 커스텀 선택창으로 교체했다. 시트·팝오버·선택 목록·전역 `+`에 절제된 글래스 효과를 적용했다.
- 더보기에서 자산 비중 막대 표시를 `기본`·`분할 라벨` 중 선택하며 기기에 기억한다.
- 홈과 포트폴리오의 시세 새로고침 버튼 아래 여백을 보완했다.
- 결정 사항은 `SPEC.md`의 화면 구조와 `UX 보완에서 정한 것 (2026-09-20)`에 반영했다.

## 검증 결과

- `cd web && npm test`: 14파일, 95개 통과
- `cd web && npm run build`: 성공
- `cd worker && npm test`: 13파일, 90개 통과
- 로컬 모바일 폭 브라우저에서 종목 보유/관심 전환, 긴 Microsoft 검색 결과, 포트폴리오 위험 영역, 투자의견 필터 선택창, AMD 커버리지 개시 시트와 가격 설명 `?`, 자산 비중 두 표시 방식을 확인했다.

## 배포할 때 순서

사용자 확인 후에만 진행한다.

1. `cd worker && npx wrangler d1 migrations apply invest --local`
2. `cd worker && npx wrangler d1 migrations apply invest --remote`
   - 이번 새 마이그레이션 `0011`은 EFFR 이름 1행 갱신 + 목표범위 시리즈 2행 upsert뿐이라 대량 쓰기가 아니다.
3. `cd worker && npx wrangler deploy`
4. 앱의 더보기 → 거시에서 `거시·실적일 지금 받기`를 실행해 목표범위 데이터를 채운다. 대량 백필이라 실행 전 예상 쓰기 행 수와 D1 일일 10만 행 한도를 다시 확인한다.
5. `main`을 이 브랜치로 fast-forward하고 push한다. Pages는 자동 배포된다.
6. 아이폰에서 아래를 확인한다.
   - 홈·포트폴리오 새로고침 여백과 자산 비중 표시 옵션
   - 종목의 보유/관심 전환 및 긴 검색명
   - 커버리지 개시 → 투자의견 업데이트 → 변천 타임라인
   - 기술적 분석 진입점과 커스텀 선택창
   - 미국 기준금리 목표범위와 별도 EFFR

## 주의

- `.dev.vars`, `portfolio.json`, 토큰/API 키를 커밋하지 않는다.
- 현재 로컬 검증용 서버는 종료한다. 다시 띄울 때 프론트는 `cd web && npm run dev -- --host 127.0.0.1`, Worker는 기존 `.dev.vars`를 사용해 `cd worker && npx wrangler dev --local --ip 127.0.0.1 --port 8787`로 실행한다.
- UI 용어만 바꿨으며 DB/API의 `views`, `tech_calls` 식별자는 그대로다.
