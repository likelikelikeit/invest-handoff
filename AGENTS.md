# AGENTS.md — 이 저장소에서 일하는 에이전트를 위한 규약

## 먼저 읽을 것
1. **`SPEC.md` 전부.** 6단계에 걸쳐 사용자와 확정한 설계다. 읽지 않고 구현을 시작하지 않는다.
2. 이 문서.
3. 착수할 마일스톤(§8)의 완료 기준.

## 사용자
- 1명. 이름은 형진. 한국어로 소통한다. CS·경영 복수전공, 투자 리서치 인턴. 코드를 읽을 수 있지만 프레임워크(Svelte)와 wrangler는 이 프로젝트에서 처음 쓴다. 새 도구를 쓰는 지점에서는 명령 한 줄과 그 이유를 짧게 설명한다.
- 창의적이고 자율적으로 결정하고 싶어 한다. **SPEC의 [확정] 항목을 바꾸려면 먼저 묻는다.** [제안] 항목은 더 나은 방법이 있으면 바꾸되 이유를 남긴다. [미정]은 구현 시점에 묻거나 미룬다.
- "이렇게 하자"보다 "여기서 갈리는데 각각 이런 대가가 있다" 식으로 선택지를 펼쳐주는 걸 선호한다.

## 절대 규칙
- **`portfolio.json`, `.dev.vars`, 토큰, API 키는 커밋하지 않는다.** `.gitignore`를 첫 커밋에 넣는다. 실제 보유 데이터는 D1과 사용자 기기에만 있다.
- **LLM은 계산하지 않는다.** RSI·밴드·비중·수익률은 코드가 계산한다. LLM 호출은 `/import`(스크린샷 읽기)에만 있다.
- 사용자에게 보이는 텍스트는 전부 **한국어**. 코드 식별자는 영어.
- 투자의견(`views`)은 **덮어쓰지 않는다.** 업데이트는 새 행. 편집은 허용하되 `edited_at`을 채운다.
- 기술적 판정(`tech_calls`)은 투자의견·성과평가와 섞지 않는다. 예측형 문구("며칠 기다려라")를 쓰지 않는다.
- 상승은 빨강, 하락은 파랑. 카드 그리드를 만들지 않는다. 금액은 축약하지 않는다.

## 스택과 도구
- 프론트 `web/`: Svelte + Vite. 배포는 GitHub Actions → GitHub Pages.
- 백엔드 `worker/`: Cloudflare Worker, `wrangler`. DB는 D1, 마이그레이션은 `worker/migrations/NNNN_*.sql` 순번.
- 차트: TradingView Lightweight Charts(가격·밴드), 직접 SVG(도넛 등).
- 테스트: 계산 로직은 `web/src/lib/calc/`에 순수 함수로 두고 Vitest. UI 스모크는 jsdom.
- 폰트 Pretendard. 토큰은 `SPEC.md` §6.3.

## 작업 방식
- 마일스톤 순서(§8)를 따른다. 앞 단계 완료 기준을 만족하기 전에 다음 단계 기능을 섞지 않는다.
- 한 마일스톤 안에서도 작게 자주 커밋한다. 커밋 메시지는 한국어 또는 영어 무관, 무엇을 왜 바꿨는지.
- 외부 소스(야후 quoteSummary, 네이버)는 비공식이다. 어댑터를 `worker/src/sources/`에 격리하고, 실패 시 앱이 죽지 않고 "데이터 없음"으로 표시되게 한다.
- 시세는 지연 시세다. 화면에 기준 시각을 항상 표시한다.
- 크론은 UTC 고정(§7.3). 서머타임 계산을 코드에 넣지 않는다.
- Worker 무료 플랜 제약(CPU 10ms, 서브요청 50)을 넘을 작업은 배치로 나눈다.
- 스크린샷 프롬프트와 `normalize`/`merge` 로직은 기존 `worker.js`/`index.html`에서 가져온다. 이미 검증된 코드다. 다시 짜지 말고 이식한다.

## 마일스톤을 시작할 때
1. `SPEC.md` §8에서 해당 행의 완료 기준을 다시 읽는다.
2. 필요한 [미정] 항목이 있으면 시작 전에 한 번에 모아서 묻는다.
3. 끝나면 완료 기준을 어떻게 확인했는지(실행 결과, 스크린샷, 테스트) 짧게 보고한다.
4. 바꾼 [제안] 항목이 있으면 `SPEC.md`도 함께 고친다. 문서와 코드가 어긋나게 두지 않는다.

## 넘겨받은 파일
- `worker.js` — 현재 Cloudflare에 배포되어 동작 중인 Worker. 마일스톤 1에서 `worker/src/`로 흡수.
- `index.html` — 순수 JS 시뮬레이터. 로직과 디자인 톤의 기준점. 마일스톤 2에서 Svelte로 이식.
- `portfolio.json` — 사용자 실제 보유. **커밋 금지.** D1 시드 전용.
